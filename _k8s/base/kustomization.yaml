apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - redis.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: demoapp
